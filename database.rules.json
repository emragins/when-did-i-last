{
  "rules": {
    "$uid": {
      ".read": "$uid === auth.uid",
      ".write": "$uid === auth.uid",
      "actions": {
        "$actionId":{
        	".validate" : "newData.isString() && newData.val().length > 0"
        }
      }
    }
  }
}
